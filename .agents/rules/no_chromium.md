---
description: Do not use the browser_subagent tool.
---
# No Chromium
The `browser_subagent` tool currently has an issue where it opens a browser and does nothing, then opens another one. Do not use the `browser_subagent` tool. If you need to check something in the browser, ask the user to provide logs or describe what they see.
